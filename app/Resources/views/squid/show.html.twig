{% extends 'base.html.twig' %}

{% block body %}
    <h1>Squid</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ squid.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ squid.name }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ squid.color }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('squid_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('squid_edit', { 'id': squid.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
